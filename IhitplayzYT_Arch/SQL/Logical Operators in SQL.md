SQL supports AND OR and NOT logical operators to be used in condition queries.
##### Usage:
```sql
SELECT * FROM Employees WHERE Employees.Salary > 90000 and Employees.id < 1000 or id == 2000 and not id == -1;
```
[[Constraint]]
[[SQL_Node]] : Return to  SQL Root Node.